- clean: ['~']

- link:
    ~/.profile: 
        force: true
        path: profile
    ~/.gitconfig:
        force: true
        path: gitconfig
    ~/.ssh/config:
        path: ssh/config
        create: true
    ~/.zprofile: zprofile
    ~/.zshenv: zshenv
    ~/.zshrc: 
        force: true
        path: zshrc
    ~/.p10k.zsh: p10k

- shell:
  - [./aptinstall.sh]
  - sudo chsh -s $(which zsh)

